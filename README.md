# Subnetting-Tool
A subnetting tool divides a large IP network into smaller subnets. It takes an IP address and subnet mask (or CIDR) as input and outputs details like network address, broadcast address, host range, and number of hosts. It helps optimize IP usage and simplifies network management.
